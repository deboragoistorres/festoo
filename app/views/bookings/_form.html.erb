<%= simple_form_for [@product, @booking] do |f| %>

  <%= f.input :b_date, as: :string, input_html: { class: 'datepicker' }, label: 'Data de Início'  %>

  <%= f.input :e_date, as: :string, input_html: { class: 'datepicker' }, label: 'Data de entrega'  %>
  <%= f.label 'Quantidade a ser agendada' %>
  <%= f.input :sched_qty, label: false %>

  <%= f.submit 'Agendar' %>
<% end %>
